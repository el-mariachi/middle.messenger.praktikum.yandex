{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "pwa-node",
      "request": "launch",
      "name": "Launch Program",
      "skipFiles": ["<node_internals>/**"],
      "program": "${workspaceFolder}/src/utils/split.ts",
      "preLaunchTask": "tsc: build - tsconfig_debug.json",
      "outFiles": ["${workspaceFolder}/dist/**/*.js"]
    }
  ]
}
